#include <unistd.h>
#inclide <stdlib.h>

/**
* main - Prints program that prints the environment using the global variable environ
*
*/

int main(int argc, char**argv,char**environ)
{
for (int i=0; environ[i] !=Null; i++)
{
printf("%d: %s\n",i,environ[i]);
}
}
