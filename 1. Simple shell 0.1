#include <stdio.h>


/**
 * main - 
 *
 * Return: Always 0.
 */
int main(int argc, char*argv[])
{
    printf("Before execve\n");
    printf("Before execve\n");
    printf("Before execve\n");
    printf("Before execve\n");
    {
        perror("Error:");
    }
    printf("After execve\n");
    return (0);
}
