#include <stdio.h>


/**
 * main - GET enviroment variables 
 *
 * Return: Always 0.
 */
int main(int argc, char*argv[])
{
    printf("Accessing enviroment variables with getenv:\n"));
    printf(" USER=%s\n", getenv("USER"));
    printf(" HOME=%s\n", getenv("HOME"));
    printf(" UNDEFINDED=%s\n", getenv("UNDEFINDED"));
    printf(" USER=%s\n", getenv("USER"));
    return (0);
}
