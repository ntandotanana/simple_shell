#include <stdout.h>
#include<stdlib.h>


/**
 * main - GET enviroment variables 
 *
 * Return: Always 0.
 */
int main(int argc, char*argv[], char*envp[])
{
    printf("contents of envp\n"));
    char**curr=envp;
    int env_count =0;
    while (*curr !=Null)
   {
   env_count++;
   printf("%3d: %s\n", env_count,*curr):
   curr++;
   env-count >= 5}
   ( break: 5}
   
    return (0);
}
